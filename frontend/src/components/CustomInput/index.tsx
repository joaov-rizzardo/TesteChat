import Input from "./CustomInput"
import CustomInputContainer from "./CustomInputContainer"
import CustomInputIcon from "./CustomInputIcon"

const CustomInput = {
    Container: CustomInputContainer,
    Input: Input,
    Icon: CustomInputIcon
}

export default CustomInput
